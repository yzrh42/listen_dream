<div class="relative flex flex-col items-center mb-10">

  <!-- タイトルを右上に配置 -->
  <h1 class="absolute top-0 left-0 text-2xl font-bold m-4">変な夢共有</h1>

  <!-- 投稿カード一覧 -->
  <div class="mt-20 flex flex-col items-center">
    <% @posts.each do |post| %>
      <div class="card bg-base-100 w-96 shadow-sm mb-6">
        <div class="card-body">
          <p class="text-right text-sm text-gray-500">
            <%= post.date.strftime("%Y年%m月%d日") %>
          </p>
          <p class="text-left mt-2">
            <%= post.body %>
          </p>
          <div class="mt-4 flex justify-end gap-2">
            <%= link_to '編集', edit_post_path(post), class: 'btn btn-primary btn-sm' %>
            <%= link_to '削除', post_path(post), class: 'btn btn-secondary btn-sm', data: { turbo_method: :delete } %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>