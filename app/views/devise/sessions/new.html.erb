<div class="flex flex-grow mb-10 items-center justify-center">

    <%= form_with model: resource, url: session_path(resource_name), local: true, data: { turbo: false }, local: true do |f| %>
      <fieldset class="fieldset bg-base-200 border-base-300 rounded-box w-xs border p-4">
        <legend class="fieldset-legend">Login</legend> 

        <label class="label">Email</label>
        <%= f.email_field :email, class: "input", placeholder: "Email", autofocus: true, autocomplete: "email" %>

        <label class="label">Password</label>
        <%= f.password_field :password, class: "input", placeholder: "Password", autocomplete: "current-password" %>

        <div class="mt-4">
          <%= f.submit "Login", class: 'btn btn-neutral w-full' %>
        </div>
        <div class="mt-4">
          <%= link_to 'Sign up', new_user_registration_path, class: "btn btn-neutral w-full" %>
        </div>
      </fieldset>
    <% end %>

</div>